# Configuration
db.url=jdbc:h2:mem:orderflow;DB_CLOSE_DELAY=-1
db.user=sa
db.password=

# Thread pool
executor.poolSize=2
executor.queueSize=10000

# Cache
cache.maxSize=100000
cache.ttlSeconds=300

# Output
output.directory=./output
